Fork of `guillaumeaubert/gmvault-docker` with support for multiple addresses.

`GMVAULT_EMAIL_ADDRESSES` is a comma seperated list of email addresses.

The db & log for each address is stored in /data/$ADDRESS/